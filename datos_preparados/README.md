# Datos preparados pendientes

Work 1 debe sustituir este marcador por:

- `municipios.csv`
- `demografia.csv`
- `servicios.csv`
- `municipios.geojson` (opcional si `municipios.csv` ya contiene latitud/longitud)
- `metadata_sources.json`

No copiar aquí los fixtures `TEST_*` como evidencia. Antes de crear una versión del portal, ejecutar la
validación y actualizar `FUENTES.md`.
